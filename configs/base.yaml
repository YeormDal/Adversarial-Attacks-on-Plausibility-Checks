data_root: C:\v2x_repro\data\veremi
resample_hz: 10

l1:
  tau_v: 0.5
  tau_a: 1.0
  tau_j: 2.0

l2:
  kappa: 0.5
  eps_v: 0.1

attack:
  epsilon: 0.1
  K: 5
  jerkCap: 2.0
  T0: 3.0
  rollback: 0.7
  dt: 0.1

split:
  train: [S0, S1, S2, S5]
  val: [S3]
  test: [S4]

seed: 42

plausibility:
  dt: 0.1         # 10 Hz 샘플링 간격 (초)
  eps_v: 0.05     # 0 속도 근처 수치불안정 보호 임계값 (m/s)
  tau_v: 0.5      # |Δv| 제한 (프레임간 속도 변화량 기반)
  tau_a: 1.0      # |a| L1 임계값 (m/s^2)
  tau_j: 2.0      # |jerk| L1 임계값 (m/s^3)
  kappa: 0.5      # |a_lat - v*r| L2 허용오차 (m/s^2)

  # NOTE: 데이터 컬럼명 매핑(필요 시 수정)
  col_time: t
  col_track: id
  col_v: v
  col_a: a
  col_r: r
  col_alat: alat